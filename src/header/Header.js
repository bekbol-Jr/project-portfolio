import React from 'react';

const Header = () => {
    return (
        <section id="portfolio">
            <div className="container">

            </div>
        </section>
    );
};

export default Header;